Codes with:
1. In "spin-J field massless.nb" or "spin-J field massive.nb" there are the calculations for Bogoliubov coefficients with Feynman rules of the vertex interaction gravity-matter and the differential number density produced for those fields.
2. In "spin-J field TT.nb" it's calculated the 2-point function <TT> (where T is the energy-stress tensor) needed to derive the vacuum persistence probability.
3. Other codes contains Definition (used in the calculations) for spinors, polarization vectors and tensors or Checks of proprerties.
